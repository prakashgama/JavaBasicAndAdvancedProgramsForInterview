package a;

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;

public class Questions {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
//reverseOrder names
//fileter P name 
		Integer x=new Integer(123);
		Integer b=x+0;
		System.out.println(b==x);
		/*
		 * List<String> names=new ArrayList<String>(); names.add("Prakash");
		 * names.add("Piyush"); names.add("Akash"); names.add("Raj"); names.add("Ajay");
		 * 
		 * 
		 * 
		 * System.out.println(names.stream().sorted((a,b)->b.compareTo(a)).collect(
		 * Collectors.toList()));
		 * 
		 * System.out.println(names.stream().filter(x->x.charAt(0)=='P').collect(
		 * Collectors.toList()));
		 */		
		
	}

}
